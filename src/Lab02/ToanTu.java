package Lab02;

public class ToanTu {
    public static void main(String[] args) {
        // TOÁN TỬ TÍNH TOÁN
        // +, -, *, /, %(chia lấy dư), ++(tăng giá trị lên 1 đơn vị), --(giảm giá trị đi 1 đơn vị)
        // = (gán giá trị), += n(lấy giá trị cho trước + n ví dụ: x+= 3 là x=x+3), -=(lấy giá trị cho trước - n),
        // *=, /=, %=, &=(và bằng), |=, ^=(mũ bằng), >>=, <<=

        // TOÁN TỬ SO SÁNH
        // ==(so sánh bằng), !=(ko bằng), >, <, >=, <=

        // TOÁN TỬ LOGIC
        // true/false, &&, ||, !(phủ định)
    }
}
